/**
 * Page-specific JavaScript for Page ID 25962
 * Handles customizations specific to this page
 */

jQuery(document).ready(function ($) {
    
    // ==========================================
    // PAGE-SPECIFIC CUSTOMIZATIONS
    // ==========================================
    // Custom link behavior for specific page
    if ($('body').hasClass('page-id-25962')) {
        $('.darkmode.dark-header .menu-item-2371 a').attr('href', '/contact-us/');
    }

});
